import { User } from '../user/user.entity';
import { Project } from '../project/project.entity';
import { Vendor } from '../vendor/vendor.entity';

const entities = [User, Project, Vendor];

export default entities;
